# dsv4-vision-vllm

vLLM general plugin that registers `DeepseekV4VisionForCausalLM` for the pinned Anemll DSpark DeepSeek V4 runtime.

This package is not a generic stock-vLLM extension. Build and deployment instructions are maintained in the repository root README.
